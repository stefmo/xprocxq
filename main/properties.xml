<!-- mostly contains locations of dependencies, one should customize to reflect their own setup//-->
<!-- TODO: probably need to refactor at some point //-->
<p>
<dir>
    <lib>
        <saxon>lib/saxon8</saxon>
        <saxon-ext>lib</saxon-ext>
    </lib>
</dir>
<jar>
    <saxon>lib/saxon/saxon8sa.jar</saxon>
</jar>
</p>

